# Python virtual environment
venv/
.venv/

# Python cache
__pycache__/
*.py[cod]

# Recorded data (poses, logs, measurement history)
*.csv
logs/

# Images produced by the scripts (--png): regenerated on demand
pool_layout_3d.png
demo_kalman.png
kalman_live_plots_demo.png
kalman_live_plots_session.png

# IDE files
.vscode/
.idea/

# Calibration files written per machine, never shared
calibration/mountings/
calibration/montages/

# The physical mounting of THIS machine (bare_air / tube_air / tube_water).
# Deliberately not versioned: the pool PC and the office laptop do not have
# the same camera in the same medium, and a git pull must never change the
# other machine's setting.
calibration/local_mounting.txt
calibration/montage_local.txt

# Evidence screenshots captured by demo_distance.py
calibration/evidence/
proofs/
preuves/
